import { motion } from 'framer-motion';
import { Area, Bar, BarChart, CartesianGrid, Cell, ComposedChart, Line, ReferenceLine, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import { Panel } from '../components/Panel';
import { Note } from '../components/Note';
import { ax, fade, MONO, T, tip } from '../theme';

export function OverviewView({ data }) {
  const { weekly, calFlags, grades, specialsNorm, topClasses, behaviors } = data;
  const maxRate = Math.max(...weekly.map((d) => d.rate));
  return <motion.div {...fade} className="space-y-3">{/* kept exact visual structure */}
    <div className="grid gap-3 xl:grid-cols-[1.6fr_1fr]"><Panel title="Weekly incident rate" sub="Incidents per school day (WSA calendar-adjusted) · dashed = 3-week moving avg"><div className="h-56"><ResponsiveContainer width="100%" height="100%"><ComposedChart data={weekly} margin={{ top: 4, right: 4, bottom: 0, left: -12 }}><defs><linearGradient id="ag" x1="0" x2="0" y1="0" y2="1"><stop offset="0%" stopColor={T.accent} stopOpacity={0.2} /><stop offset="100%" stopColor={T.accent} stopOpacity={0.01} /></linearGradient></defs><CartesianGrid stroke={T.grid} vertical={false} /><XAxis dataKey="week" {...ax} tick={{ ...ax.tick, fontSize: 9 }} /><YAxis {...ax} /><Tooltip content={({ active, payload, label }) => { if (!active || !payload?.length) return null; const d = payload[0]?.payload; return <div style={{ background: T.surface, border: `1px solid ${T.borderHi}`, borderRadius: 6, padding: '8px 10px', fontSize: 11 }}><div style={{ color: T.textBright, ...MONO, marginBottom: 3 }}>{label}</div><div style={{ color: T.accent }}>Rate: <b>{d?.rate?.toFixed(2)}/day</b></div><div style={{ color: T.amber }}>3-wk avg: {d?.ma?.toFixed(2)}</div><div style={{ color: T.textDim }}>Cal. days: {d?.days}</div>{d?.note && <div style={{ color: T.amber, marginTop: 3, paddingTop: 3, borderTop: `1px solid ${T.border}` }}>{d.note}</div>}</div>; }} /><ReferenceLine y={5.5} stroke={T.muted} strokeDasharray="3 3" label={{ value: 'dataset avg', fill: T.textDim, fontSize: 9, position: 'insideTopRight' }} /><Area type="monotone" dataKey="rate" stroke={T.accent} strokeWidth={2} fill="url(#ag)" dot={({ cx, cy, payload: p }) => { if (calFlags.lowConf.includes(p.week)) return <circle key="lc" cx={cx} cy={cy} r={3} fill={T.surfaceHi} stroke={T.muted} strokeWidth={1.5} />; if (p.rate === maxRate) return <circle key="sp" cx={cx} cy={cy} r={5} fill={T.alert} stroke={T.bg} strokeWidth={2} />; if (calFlags.preBreak.includes(p.week)) return <circle key="pb" cx={cx} cy={cy} r={4} fill="none" stroke={T.alert} strokeWidth={1.5} />; return <circle key={cx} cx={cx} cy={cy} r={2.5} fill={T.accent} />; }} /><Line type="monotone" dataKey="ma" stroke={T.amber} strokeWidth={1.5} strokeDasharray="5 4" dot={false} /></ComposedChart></ResponsiveContainer></div><div className="mt-2 flex flex-wrap gap-x-4 gap-y-1 text-[10px]" style={{ color: T.textDim }}><span className="flex items-center gap-1.5"><span className="inline-block w-3 h-0.5" style={{ background: T.accent }} /> Rate/day</span><span className="flex items-center gap-1.5"><span className="inline-block w-4 border-t border-dashed" style={{ borderColor: T.amber }} /> 3-wk avg</span><span className="flex items-center gap-1.5"><span className="inline-block w-2 h-2 rounded-full" style={{ background: T.alert }} /> Highest week</span><span className="flex items-center gap-1.5"><span className="inline-block w-2 h-2 rounded-full border" style={{ borderColor: T.alert, background: 'transparent' }} /> Pre-spring break</span><span className="flex items-center gap-1.5"><span className="inline-block w-2 h-2 rounded-full border" style={{ borderColor: T.muted, background: T.surfaceHi }} /> Low confidence</span></div><div className="mt-3 space-y-0"><Note>Feb W6 shows 1.2/day but the WSA calendar confirms a full 5-day school week with no holidays or workdays. Only 1 of 5 days had any incident logged. Do not read W6 as a behavioral improvement.</Note><Note>W14 (Mar 30–Apr 1) is the highest week at 10.33/day. This is a 3-day partial week — spring break started Apr 3. Pre-break weeks are a common behavioral spike period.</Note></div></Panel>
      <Panel title="Incidents by grade" sub="Raw count"><div className="h-56"><ResponsiveContainer width="100%" height="100%"><BarChart data={grades} margin={{ top: 4, right: 4, bottom: 0, left: -14 }}><CartesianGrid stroke={T.grid} vertical={false} /><XAxis dataKey="grade" {...ax} /><YAxis {...ax} /><Tooltip {...tip} /><Bar dataKey="n" radius={[3, 3, 0, 0]}>{grades.map((d, i) => <Cell key={d.grade} fill={T.series[i]} />)}</Bar></BarChart></ResponsiveContainer></div></Panel></div>
    <div className="grid gap-3 xl:grid-cols-[1.1fr_1fr]"><Panel title="Incidents per logging day by specials" sub="Normalized by days with at least one logged incident"><div className="h-44"><ResponsiveContainer width="100%" height="100%"><BarChart data={specialsNorm} layout="vertical" margin={{ left: 6, right: 4 }}><CartesianGrid stroke={T.grid} horizontal={false} /><XAxis type="number" {...ax} domain={[0, 5]} /><YAxis type="category" dataKey="name" {...ax} width={80} /><Tooltip {...tip} formatter={(v) => [`${v.toFixed(2)} / day`]} /><Bar dataKey="perDay" radius={[0, 3, 3, 0]}>{specialsNorm.map((d, i) => <Cell key={d.name} fill={T.series[i]} />)}</Bar></BarChart></ResponsiveContainer></div></Panel>
      <Panel title="Top classes by incident count" sub="Normalized class names · three-way tie at 22"><div className="space-y-2 mt-1">{topClasses.map((row, i) => <div key={row.cls}><div className="flex items-center justify-between mb-1"><div className="flex items-center gap-2"><span className="text-[10px] w-4 text-right" style={{ color: T.textDim, ...MONO }}>{i + 1}</span><span className="text-[11px]" style={{ color: T.text }}>{row.cls}</span></div><span className="text-[11px] font-semibold" style={{ color: T.textBright, ...MONO }}>{row.n}</span></div><div className="h-1 rounded-full overflow-hidden ml-6" style={{ background: T.border }}><motion.div className="h-full rounded-full" style={{ background: i < 3 ? T.alert : T.accent }} initial={{ width: 0 }} animate={{ width: `${(row.n / 22) * 100}%` }} transition={{ delay: i * 0.03 + 0.15, duration: 0.4 }} /></div></div>)}</div></Panel></div>
    <Panel title="Observed behavior types" sub="All 248 records · 'Unspecified' = no behavior type tagged (70 records, 28%)" right={<span className="text-[10px] px-1.5 py-0.5 rounded font-semibold" style={{ background: T.alert + '18', color: T.alert }}>28% untagged</span>}><div className="h-72"><ResponsiveContainer width="100%" height="100%"><BarChart data={behaviors} layout="vertical" margin={{ left: 14, right: 8 }}><CartesianGrid stroke={T.grid} horizontal={false} /><XAxis type="number" {...ax} /><YAxis type="category" dataKey="type" {...ax} width={128} /><Tooltip {...tip} /><Bar dataKey="n" radius={[0, 3, 3, 0]}>{behaviors.map((d, i) => <Cell key={d.type} fill={d.type === 'Unspecified' ? T.muted : T.series[i % T.series.length]} />)}</Bar></BarChart></ResponsiveContainer></div></Panel>
  </motion.div>;
}
